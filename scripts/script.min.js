import{createApp,reactive}from"https://unpkg.com/petite-vue?module";const util=reactive({fetchData:api=>fetch(api).then(res=>res.json()),firstLetterUppercase:str=>str.charAt(0).toUpperCase()+str.slice(1)}),App={util:util,max:905,pokemons:[],types:[],currentType:"",get filterPokemons(){return this.currentType?this.pokemons.filter(pokemon=>pokemon.types.includes(this.currentType)):this.pokemons},get progress(){return`${this.pokemons.length/this.max*100}%`},async init(){const pokemons=await fetch("../data/PokeApi.json").then(res=>res.json());this.pokemons=pokemons,pokemons.forEach(pokemon=>pokemon.types.forEach(type=>{this.types.includes(type)||this.types.push(type)}))}};createApp(App).mount();