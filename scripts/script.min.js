import{createApp,reactive}from"https://unpkg.com/petite-vue?module";const util=reactive({fetchData:async api=>await fetch(api).then(res=>res.json()),firstLetterUppercase:str=>str.charAt(0).toUpperCase()+str.slice(1),getProgress:(numerator,denominator)=>`${(numerator/denominator*100).toFixed()}%`}),App={util:util,max:905,pokemons:[],types:[],currentType:"",get progress(){return util.getProgress(this.pokemons.length,this.max)},async init(){const pokemons=await util.fetchData("../data/PokeApi.json");this.pokemons=pokemons,pokemons.forEach(pokemon=>pokemon.types.forEach(type=>{this.types.includes(type)||this.types.push(type)}))}};createApp(App).mount();